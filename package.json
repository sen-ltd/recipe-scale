{
  "name": "recipe-scale",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recipe ingredient scaler with fraction parsing and unit conversion.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
